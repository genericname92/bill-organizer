<% if (window.CURRENT_USER_ID === parseInt(bill.escape("owner_id"))){ %>
  <button
    type="button" class="killComment close" title="Delete this comment!">&times;
  </button>
<% } %>
<%= comment.escape("body") %>
<br>
<div class="timestamps">
<%= comment.escape("timeAgo") %> ago  Posted by: <%=comment.escape("user") %>
</div>
